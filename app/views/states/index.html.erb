<p id="notice"><%= notice %></p>

<h1>Estados</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>País</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @states.each do |state| %>
      <tr>
        <td><%= state.name %></td>
        <td><%= state.country.name %></td>
        <td><%= link_to 'Mostrar', state %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

